# Hyprpaper random wallpapers from the synced wallpapers config directory.
# The repo target `wallpapers/configs` syncs to ~/.config/wallpapers.

ipc = on
splash = false

wallpaper {
    monitor =
    path = ~/.config/wallpapers
    fit_mode = cover
    timeout = 900
    order = random
    recursive = true
}
